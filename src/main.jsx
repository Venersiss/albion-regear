import React, { useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import './styles.css'

const icons = {
  grid: <><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></>,
  plan: <><path d="M6 3h12a2 2 0 0 1 2 2v14l-4-2-4 2-4-2-4 2V5a2 2 0 0 1 2-2Z"/><path d="M8 8h8M8 12h6"/></>,
  users: <><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></>,
  box: <><path d="m21 8-9-5-9 5 9 5 9-5Z"/><path d="m3 8 9 5 9-5M3 8v8l9 5 9-5V8M12 13v8"/></>,
  settings: <><path d="M12 15.5a3.5 3.5 0 1 0 0-7 3.5 3.5 0 0 0 0 7Z"/><path d="m19.4 15 .1.1a2 2 0 1 1-2.8 2.8l-.1-.1a2 2 0 0 0-3.4 1.4v.3a2 2 0 1 1-4 0v-.2A2 2 0 0 0 5.8 18l-.1.1a2 2 0 1 1-2.8-2.8l.1-.1A2 2 0 0 0 1.6 12a2 2 0 1 1 0-4h.2a2 2 0 0 0 1.4-3.4l-.1-.1a2 2 0 1 1 2.8-2.8l.1.1A2 2 0 0 0 9.4.4h.2a2 2 0 1 1 4 0v.2A2 2 0 0 0 17 2l.1-.1a2 2 0 1 1 2.8 2.8l-.1.1A2 2 0 0 0 21.2 8h.2a2 2 0 1 1 0 4h-.2a2 2 0 0 0-1.8 3Z"/></>,
  bell: <><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/></>,
  search: <><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></>,
  plus: <><path d="M12 5v14M5 12h14"/></>,
  chevron: <path d="m9 18 6-6-6-6"/>,
  arrow: <><path d="M5 12h14M13 6l6 6-6 6"/></>,
  check: <path d="m5 12 4 4L19 6"/>,
  more: <><circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/></>,
  swords: <><path d="m14.5 5.5 4-4 1 1-4 4M13 7l4 4M5 19l6-6M4 20l1-1M3 15l6 6M12 12 4 4"/><path d="m14 4 6 6M4 20h4"/></>,
  close: <><path d="M6 6l12 12M18 6 6 18"/></>,
  eye: <><path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6S2 12 2 12Z"/><circle cx="12" cy="12" r="2.5"/></>,
}

function Icon({ name, size = 18, className = '' }) {
  return <svg className={`icon ${className}`} width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{icons[name]}</svg>
}

const initialItems = [
  { name: 'Knight Helmet', category: 'Head', chest: 'C-04', stock: '18 / 24', percentage: 75, tone: 'good' },
  { name: 'Royal Jacket', category: 'Armor', chest: 'C-04', stock: '21 / 24', percentage: 88, tone: 'good' },
  { name: 'Heavy Mace', category: 'Weapon', chest: 'C-07', stock: '5 / 8', percentage: 62, tone: 'warn' },
  { name: 'Leering Cane', category: 'Off hand', chest: 'C-07', stock: '3 / 8', percentage: 38, tone: 'low' },
  { name: 'Soldier Boots', category: 'Boots', chest: 'C-04', stock: '23 / 24', percentage: 96, tone: 'good' },
]

const roleOptions = ['Tank', 'Support', 'Healer', 'DPS', 'Bomb', 'Caller']

const initialMembers = [
  { name: 'Kestrel', role: 'Caller', guild: 'Coup De Grace', avatar: 'K', tone: 'violet', status: 'Ready', chest: 'C-04', last: 'Today, 08:42', issuedBy: 'Jasper D.' },
  { name: 'Mirael', role: 'Support', guild: 'Coup De Grace', avatar: 'M', tone: 'blue', status: 'Open regear', chest: 'C-07', last: 'Yesterday, 22:10', issuedBy: 'Jasper D.', deathNote: 'Fell in Roads · yesterday, 22:10' },
  { name: 'Oaken', role: 'Tank', guild: 'Coup De Grace', avatar: 'O', tone: 'green', status: 'Ready', chest: 'C-02', last: 'Today, 09:15', issuedBy: 'Mirael' },
  { name: 'Sablefox', role: 'DPS', guild: 'Coup De Grace', avatar: 'S', tone: 'orange', status: 'Open regear', chest: 'C-09', last: '2 days ago', issuedBy: 'Jasper D.', deathNote: 'Died in Crystal League · 2 days ago' },
  { name: 'Tallow', role: 'Healer', guild: 'Coup De Grace', avatar: 'T', tone: 'rose', status: 'Ready', chest: 'C-03', last: 'Today, 07:58', issuedBy: 'Oaken' },
  { name: 'Rook', role: 'Bomb', guild: 'Coup De Grace', avatar: 'R', tone: 'teal', status: 'Open regear', chest: 'C-11', last: '3 days ago', issuedBy: 'Mirael', deathNote: 'Died on CTA · 3 days ago' },
]

const plans = [
  { title: 'Avalonian Roads — 13 Sep', subtitle: '20:00 UTC · 8 members', status: 'Upcoming', progress: '0 deaths reported', cost: '840k silver', date: '13 SEP', accent: 'ember' },
  { title: 'Crystal League — 11 Sep', subtitle: '21:30 UTC · 6 members', status: 'In progress', progress: '4 deaths · 3 closed', cost: '620k silver', date: '11 SEP', accent: 'blue' },
  { title: 'Open World roam — 08 Sep', subtitle: '19:00 UTC · 12 members', status: 'Complete', progress: '12 deaths · 12 closed', cost: '1.1m silver', date: '08 SEP', accent: 'green' },
]

function App() {
  const [active, setActive] = useState('Dashboard')
  const [members, setMembers] = useState(initialMembers)
  const [showMemberModal, setShowMemberModal] = useState(false)
  const [showPlanModal, setShowPlanModal] = useState(false)
  const [showDeathModal, setShowDeathModal] = useState(false)
  const [showItemModal, setShowItemModal] = useState(false)
  const [items, setItems] = useState(initialItems)
  const [query, setQuery] = useState('')
  const [toast, setToast] = useState('')

  const filteredMembers = useMemo(() => members.filter((m) => m.name.toLowerCase().includes(query.toLowerCase()) || m.role.toLowerCase().includes(query.toLowerCase())), [members, query])
  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const markRegeared = (name) => { setMembers((current) => current.map((member) => member.name === name ? { ...member, status: 'Ready', last: 'Just now', issuedBy: 'Jasper D.' } : member)); notify(`${name} marked as regeared`) }
  const reportDeath = ({ memberName, note, chest, role, items: requestedItems }) => { setMembers((current) => current.map((member) => member.name === memberName ? { ...member, status: 'Open regear', last: 'Open regear · just now', issuedBy: 'Unassigned', deathNote: note, chest, regearRole: role, regearItems: requestedItems } : member)); setShowDeathModal(false); notify(`${memberName} added to the regear queue`) }
  const updateMemberChest = (name, chest) => { const nextChest = chest.trim() || 'Unassigned'; setMembers((current) => current.map((member) => member.name === name ? { ...member, chest: nextChest } : member)); notify(`${name}'s issue chest updated to ${nextChest}`) }
  const updateItemChest = (name, chest) => { const nextChest = chest.trim() || 'Unassigned'; setItems((current) => current.map((item) => item.name === name ? { ...item, chest: nextChest } : item)); notify(`${name} moved to ${nextChest}`) }
  const addItem = (item) => { setItems((current) => current.some((existing) => existing.name.toLowerCase() === item.name.toLowerCase()) ? current : [...current, item]); setShowItemModal(false); notify(`${item.name} added to the item catalog`) }
  const addCatalogItem = (name) => { const item = { name, category: 'Custom', chest: 'Unassigned', stock: '0 / 0', percentage: 0, tone: 'low' }; setItems((current) => current.some((existing) => existing.name.toLowerCase() === name.toLowerCase()) ? current : [...current, item]); notify(`${name} added to the item catalog`); return item }

  return <div className="app-shell">
    <Sidebar active={active} onNavigate={setActive} />
    <main className="main-content">
      <Topbar query={query} setQuery={setQuery} onNotify={notify} />
      {active === 'Dashboard' && <Dashboard members={members} onOpenMember={() => setShowMemberModal(true)} onOpenPlan={() => setShowPlanModal(true)} onOpenDeath={() => setShowDeathModal(true)} onNavigate={setActive} onMark={markRegeared} />}
      {active === 'CTA events' && <Plans onOpenPlan={() => setShowPlanModal(true)} onMark={markRegeared} />}
      {active === 'Members' && <Members members={filteredMembers} onOpenMember={() => setShowMemberModal(true)} onMark={markRegeared} onUpdateChest={updateMemberChest} />}
      {active === 'Armory' && <Armory items={items} onNotify={notify} onAddItem={() => setShowItemModal(true)} onUpdateChest={updateItemChest} />}
      {active === 'Settings' && <Settings onNotify={notify} />}
      {active === 'Member view' && <MemberView />}
    </main>
    {showMemberModal && <MemberModal onClose={() => setShowMemberModal(false)} onSave={(member) => { setMembers((current) => [...current, member]); setShowMemberModal(false); notify(`${member.name} added to roster`) }} />}
    {showPlanModal && <PlanModal onClose={() => setShowPlanModal(false)} onSave={(plan) => { setShowPlanModal(false); notify(`${plan.name} created · ${plan.budget} silver budget`) }} />}
    {showDeathModal && <DeathModal members={members} items={items} onAddItem={addCatalogItem} onClose={() => setShowDeathModal(false)} onSave={reportDeath} />}
    {showItemModal && <ItemModal onClose={() => setShowItemModal(false)} onSave={addItem} />}
    {toast && <div className="toast"><span className="toast-dot" />{toast}</div>}
  </div>
}

function Sidebar({ active, onNavigate }) {
  const nav = [['Dashboard', 'grid'], ['CTA events', 'plan'], ['Members', 'users'], ['Armory', 'box']]
  return <aside className="sidebar">
    <div className="brand"><div className="brand-mark">A<span>R</span></div><div><div className="brand-name">Albion <em>Regear</em></div><div className="brand-caption">COUP DE GRACE</div></div></div>
    <div className="sidebar-rule" />
    <div className="nav-label">Workspace</div>
    <nav>{nav.map(([label, icon]) => <button key={label} className={`nav-item ${active === label ? 'active' : ''}`} onClick={() => onNavigate(label)}><Icon name={icon} size={17} /><span>{label}</span>{label === 'CTA events' && <span className="nav-count">3</span>}</button>)}</nav>
    <div className="sidebar-public"><button className={`nav-item ${active === 'Member view' ? 'active' : ''}`} onClick={() => onNavigate('Member view')}><Icon name="eye" size={17} /><span>Member view</span></button></div>
    <div className="sidebar-bottom">
      <button className={`nav-item ${active === 'Settings' ? 'active' : ''}`} onClick={() => onNavigate('Settings')}><Icon name="settings" size={17} /><span>Settings</span></button>
      <div className="user-card"><div className="avatar avatar-admin">JD</div><div className="user-meta"><strong>Jasper D.</strong><span>Administrator</span></div><button className="icon-button"><Icon name="more" size={17} /></button></div>
      <div className="version">v0.1 prototype <span>·</span> Supabase ready</div>
    </div>
  </aside>
}

function Topbar({ query, setQuery, onNotify }) {
  return <header className="topbar"><div className="mobile-brand"><div className="brand-mark">A<span>R</span></div><strong>Albion <em>Regear</em></strong></div><div className="breadcrumbs"><span>Coup De Grace</span><Icon name="chevron" size={14} /><strong>Operations room</strong></div><div className="top-actions"><label className="search"><Icon name="search" size={16} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search members..." aria-label="Search members" /><kbd>⌘ K</kbd></label><button className="notification" onClick={() => onNotify('No new alerts')} aria-label="View notifications"><Icon name="bell" size={18} /><i /></button><div className="online"><span /> Live</div></div></header>
}

function PageIntro({ eyebrow, title, description, action, onAction }) { return <div className="page-intro"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p>{description}</p></div>{action && <button className="button button-primary" onClick={onAction}><Icon name="plus" size={16} />{action}</button>}</div> }

function Dashboard({ members, onOpenMember, onOpenPlan, onOpenDeath, onNavigate, onMark }) {
  const needs = members.filter((m) => m.status === 'Open regear')
  return <div className="page dashboard-page"><PageIntro eyebrow="Tuesday, 08 September 2026" title="Good evening, Jasper." description="The guild is in good shape. Here’s who needs a fresh kit after the latest CTA." action="Report a death" onAction={onOpenDeath} />
    <div className="stat-grid"><StatCard label="Open regears" value={needs.length.toString().padStart(2, '0')} detail="casualties waiting" tone="ember" icon="swords" /><StatCard label="Ready to fight" value="18" detail="of 24 members" tone="green" icon="check" /><StatCard label="Next CTA" value="13 Sep" detail="Avalonian Roads · 20:00" tone="blue" icon="plan" /><StatCard label="Items in armory" value="248" detail="92% stocked" tone="purple" icon="box" /></div>
    <div className="dashboard-grid"><section className="panel attention-panel"><PanelHeading title="Regear queue" action="View all" onAction={() => onNavigate('Members')} /><div className="member-list">{needs.map((member) => <AttentionRow key={member.name} member={member} onMark={() => onMark(member.name)} />)}</div>{needs.length === 0 && <EmptyState text="No open regears. Everyone is ready for the next fight." />}</section><section className="panel plans-panel"><PanelHeading title="CTA schedule" action="All CTAs" onAction={() => onNavigate('CTA events')} /><div className="plan-list">{plans.slice(0, 3).map((plan) => <PlanRow key={plan.title} plan={plan} />)}</div><button className="panel-footer-link" onClick={onOpenPlan}><Icon name="plus" size={15} /> Schedule another CTA</button></section></div>
    <div className="lower-grid"><section className="panel activity-panel"><PanelHeading title="Request activity" action="Last 7 days" /><div className="chart-wrap"><div className="chart-y"><span>24</span><span>16</span><span>8</span><span>0</span></div><div className="chart"><div className="chart-gridline line-1" /><div className="chart-gridline line-2" /><div className="chart-gridline line-3" /><svg viewBox="0 0 620 190" preserveAspectRatio="none" aria-label="Regear request activity chart"><defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="#d47736" stopOpacity=".28"/><stop offset="1" stopColor="#d47736" stopOpacity="0"/></linearGradient></defs><path d="M0 141 C32 136 49 119 81 125 S128 157 160 127 S204 77 239 102 S283 135 319 88 S369 49 398 76 S436 116 467 71 S513 81 543 50 S584 39 620 15 L620 190 L0 190Z" fill="url(#area)"/><path d="M0 141 C32 136 49 119 81 125 S128 157 160 127 S204 77 239 102 S283 135 319 88 S369 49 398 76 S436 116 467 71 S513 81 543 50 S584 39 620 15" fill="none" stroke="#d47736" strokeWidth="3" vectorEffect="non-scaling-stroke"/></svg><div className="chart-x"><span>03 Sep</span><span>04 Sep</span><span>05 Sep</span><span>06 Sep</span><span>07 Sep</span><span>08 Sep</span></div></div></div><div className="chart-legend"><span><i className="legend-ember" /> Items issued <strong>86</strong></span><span><i className="legend-muted" /> Requests closed <strong>31</strong></span></div></section><section className="panel chest-panel"><PanelHeading title="Armory at a glance" action="Open armory" onAction={() => onNavigate('Armory')} /><div className="chest-map"><div className="island-grid"><span className="chest chest-a">C-01</span><span className="chest chest-b active-chest">C-04</span><span className="chest chest-c">C-07</span><span className="chest chest-d">C-09</span><span className="chest chest-e">C-11</span><span className="chest chest-f">C-14</span><div className="island-tree tree-1">✦</div><div className="island-tree tree-2">✦</div></div><div className="map-caption"><span className="map-pin" /> Most used chest <strong>C-04</strong><small>· 42 items issued</small></div></div><div className="stock-row"><div><span className="stock-label">Stock health</span><strong>92%</strong></div><div className="stock-bar"><span style={{ width: '92%' }} /></div><span className="stock-good">Good</span></div></section></div>
  </div>
}

function StatCard({ label, value, detail, tone, icon }) { return <div className={`stat-card tone-${tone}`}><div className="stat-top"><span>{label}</span><div className="stat-icon"><Icon name={icon} size={17} /></div></div><strong className="stat-value">{value}</strong><span className="stat-detail">{detail}</span><div className="stat-spark"><span /><span /><span /><span /><span /><span /></div></div> }
function PanelHeading({ title, action, onAction }) { return <div className="panel-heading"><h2>{title}</h2>{action && <button onClick={onAction}>{action}<Icon name="arrow" size={14} /></button>}</div> }
function AttentionRow({ member, onMark }) { return <div className="attention-row"><div className={`avatar avatar-${member.tone}`}>{member.avatar}</div><div className="row-primary"><strong>{member.name}</strong><span>{member.deathNote || `Death reported · Chest ${member.chest}`}</span></div><span className="item-summary">Chest {member.chest} <b>·</b> {member.regearRole || member.role} kit</span><button className="row-action" onClick={onMark}><Icon name="check" size={14} /> Mark regeared</button><button className="more-button" aria-label={`More options for ${member.name}`}><Icon name="more" size={16} /></button></div> }
function PlanRow({ plan }) { return <div className="plan-row"><div className={`plan-date date-${plan.accent}`}><strong>{plan.date.split(' ')[0]}</strong><span>{plan.date.split(' ')[1]}</span></div><div className="row-primary"><strong>{plan.title}</strong><span>{plan.subtitle}</span></div><div className={`status status-${plan.accent}`}>{plan.status}</div><span className="plan-progress">{plan.progress}<small>{plan.cost}</small></span><button className="more-button" aria-label={`More options for ${plan.title}`}><Icon name="more" size={16} /></button></div> }
function EmptyState({ text }) { return <div className="empty-state"><Icon name="check" size={20} /><span>{text}</span></div> }

function Plans({ onOpenPlan, onMark }) { return <div className="page"><PageIntro eyebrow="CTA operations calendar" title="CTA events" description="Track each CTA, then turn member deaths into clear regear requests and item hand-offs." action="Schedule CTA" onAction={onOpenPlan} /><div className="filter-bar"><div className="segmented"><button className="selected">All CTAs <span>3</span></button><button>Upcoming <span>1</span></button><button>In progress <span>1</span></button><button>Complete <span>1</span></button></div><button className="button button-ghost"><Icon name="plan" size={15} /> Calendar view</button></div><section className="panel plans-table"><div className="table-header"><span>CTA event</span><span>State</span><span>Progress / budget</span><span /></div>{plans.map((plan) => <PlanRow key={plan.title} plan={plan} />)}</section><section className="panel plan-note"><div className="note-mark"><Icon name="swords" size={19} /></div><div><strong>Regear starts when a member dies</strong><p>Report the casualty, select the replacement pieces, issue them from the assigned chest, then close the request as regeared.</p></div></section></div> }

function Members({ members, onOpenMember, onMark, onUpdateChest }) { const [roleFilter, setRoleFilter] = useState('All roles'); const roles = ['All roles', ...roleOptions]; const visibleMembers = roleFilter === 'All roles' ? members : members.filter((member) => member.role === roleFilter); const openCount = members.filter((member) => member.status === 'Open regear').length; return <div className="page"><PageIntro eyebrow="Roster management" title="Members" description="Keep the roster, default classifications, issue chests, and regear history in one place." action="Add member" onAction={onOpenMember} /><div className="filter-bar"><div className="member-count"><strong>{visibleMembers.length}</strong> active members <span>·</span> <b>{openCount} open regears</b></div><div className="filter-actions"><div className="role-filters">{roles.map((role) => <button key={role} className={roleFilter === role ? 'selected' : ''} onClick={() => setRoleFilter(role)}>{role}</button>)}</div><button className="button button-ghost">Export roster</button></div></div><section className="panel roster-table"><div className="table-header"><span>Member</span><span>Default role</span><span>Issue chest</span><span>Last regear / issuer</span><span>Status</span><span /></div>{visibleMembers.map((member) => <div className="roster-row" key={member.name}><div className="member-cell"><div className={`avatar avatar-${member.tone}`}>{member.avatar}</div><div><strong>{member.name}</strong><span>{member.guild}</span></div></div><span className={`role-cell role-${member.role.toLowerCase()}`}>{member.role}</span><span className="chest-cell"><input className="inline-chest" defaultValue={member.chest} aria-label={`Issue chest for ${member.name}`} onBlur={(e) => onUpdateChest(member.name, e.target.value)} /></span><span className="last-cell"><strong>{member.last}</strong><small>by {member.issuedBy || 'Pending'}</small></span><span className={`status status-${member.status === 'Ready' ? 'green' : 'ember'}`}>{member.status}</span><button className="more-button" aria-label={`More options for ${member.name}`} onClick={() => member.status !== 'Ready' && onMark(member.name)}><Icon name="more" size={16} /></button></div>)}</section></div> }

function Armory({ items, onNotify, onAddItem, onUpdateChest }) { return <div className="page"><PageIntro eyebrow="Guild island inventory" title="Armory" description="Know what’s in each chest before the call goes out. Low stock is flagged automatically." action="Add item" onAction={onAddItem} /><div className="armory-overview"><div className="panel inventory-total"><span className="eyebrow">Total tracked items</span><strong>248</strong><p>Across 6 chests <span>·</span> 92% healthy</p></div><div className="panel inventory-total"><span className="eyebrow">Needs restock</span><strong className="ember-text">07</strong><p>Items below your minimum threshold</p></div><div className="panel inventory-total"><span className="eyebrow">Last updated</span><strong className="time-value">18:42</strong><p>by Jasper D. <span>·</span> Today</p></div></div><section className="panel inventory-table"><div className="table-header"><span>Item</span><span>Category</span><span>Chest</span><span>Stock</span><span>Health</span><span /></div>{items.map(({ name, category, chest, stock, percentage, tone }) => <div className="inventory-row" key={name}><div className="item-cell"><div className={`item-icon item-${tone}`}><Icon name={category === 'Weapon' ? 'swords' : 'box'} size={16} /></div><strong>{name}</strong></div><span>{category}</span><span className="chest-cell"><input className="inline-chest" defaultValue={chest} aria-label={`Chest location for ${name}`} onBlur={(e) => onUpdateChest(name, e.target.value)} /></span><span>{stock}</span><div className="health-cell"><div className="mini-bar"><i className={`bar-${tone}`} style={{ width: `${percentage}%` }} /></div><span>{percentage}%</span></div><button className="more-button" onClick={() => onNotify(`${name} details opened`)} aria-label={`Open ${name}`}><Icon name="more" size={16} /></button></div>)}</section></div> }

function Settings({ onNotify }) { const [adminEmail, setAdminEmail] = useState(''); const [pendingInvites, setPendingInvites] = useState(['pending-admin@example.com']); const inviteAdmin = () => { const email = adminEmail.trim().toLowerCase(); if (!email.endsWith('@gmail.com')) { onNotify('Use a Gmail address for admin invites'); return } setPendingInvites((current) => [...current, email]); setAdminEmail(''); onNotify(`Invite sent to ${email}`) }; return <div className="page"><PageIntro eyebrow="Guild configuration" title="Settings" description="Control how your guild uses Albion Regear." /><section className="settings-grid"><div className="panel settings-nav"><button className="settings-link active">Guild profile</button><button className="settings-link">Regear defaults</button><button className="settings-link">Admin access</button><button className="settings-link">Notifications</button></div><div className="settings-stack"><div className="panel settings-form"><div className="form-section"><span className="eyebrow">Guild profile</span><h2>Make it yours</h2><p>This is how the operations room identifies your guild.</p><label>Guild name<input defaultValue="Coup De Grace" /></label><label>Server region<select defaultValue="Americas"><option>Americas</option><option>Europe</option><option>Asia</option></select></label><button className="button button-primary" onClick={() => onNotify('Guild profile saved')}>Save changes</button></div></div><div className="panel admin-access"><div className="admin-access-heading"><div><span className="eyebrow">Closed admin network</span><h2>Invite administrators</h2><p>Members never create accounts. Only an existing admin can invite a Gmail address.</p></div><span className="access-pill"><span />Invite only</span></div><div className="access-rule"><div><span>Bootstrap admin</span><strong>Configured during Supabase setup</strong></div><span className="status status-green">Owner</span></div><div className="invite-form"><label>Admin Gmail<input value={adminEmail} onChange={(e) => setAdminEmail(e.target.value)} placeholder="you@example.com" /></label><button className="button button-primary" disabled={!adminEmail.trim()} onClick={inviteAdmin}><Icon name="plus" size={15} />Send invite</button></div><div className="pending-heading"><span>Pending invitations</span><small>{pendingInvites.length}</small></div><div className="pending-list">{pendingInvites.map((email) => <div className="pending-row" key={email}><span className="pending-avatar">@</span><strong>{email}</strong><span className="status status-ember">Awaiting signup</span></div>)}</div></div></div></section></div> }

function MemberView() { return <div className="page member-view-page"><PageIntro eyebrow="Public member board · no account needed" title="Your regear history, at a glance." description="Members can check their latest regear record and issue chest here. Only administrators can create or update requests." /><div className="member-board"><section className="panel board-welcome"><div className="board-sigil"><Icon name="swords" size={24} /></div><div><span className="eyebrow">Viewing as</span><h2>Kestrel</h2><p>Caller <span>·</span> Coup De Grace</p></div><button className="button button-ghost">Change character <Icon name="chevron" size={14} /></button></section><div className="board-grid"><section className="panel board-cta"><div className="board-label"><span className="status status-green">Regeared</span><span className="eyebrow">Last CTA</span></div><h2>Avalonian Roads</h2><p>Saturday, 06 September · 20:00 UTC</p><div className="cta-rule" /><div className="cta-meta"><div><span>Regeared at</span><strong>Guild island</strong></div><div><span>Issued from</span><strong className="ember-text">C-04</strong></div></div></section><section className="panel kit-panel"><div className="panel-heading"><h2>Last regear kit</h2><span className="status status-green">Complete</span></div><div className="kit-list"><KitItem label="Weapon" value="Claymore" /><KitItem label="Off hand" value="Mistcaller" /><KitItem label="Helmet" value="Knight Helmet" /><KitItem label="Armor" value="Royal Jacket" /><KitItem label="Boots" value="Soldier Boots" /></div></section></div></div></div> }
function KitItem({ label, value }) { return <div className="kit-item"><span>{label}</span><strong>{value}</strong><Icon name="check" size={14} /></div> }

function Modal({ title, eyebrow, children, onClose }) { return <div className="modal-backdrop" onMouseDown={(e) => e.target === e.currentTarget && onClose()}><div className="modal"><div className="modal-heading"><div><span className="eyebrow">{eyebrow}</span><h2>{title}</h2></div><button className="close-button" onClick={onClose} aria-label="Close"><Icon name="close" size={19} /></button></div>{children}</div></div> }
function MemberModal({ onClose, onSave }) { const [name, setName] = useState(''); const [role, setRole] = useState('DPS'); const [chest, setChest] = useState('C-04'); return <Modal title="Add guild member" eyebrow="Roster management" onClose={onClose}><div className="form-grid"><label>Character name<input autoFocus value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Nightjar" /></label><label>Role<select value={role} onChange={(e) => setRole(e.target.value)}><option>Tank</option><option>Support</option><option>Healer</option><option>DPS</option><option>Bomb</option><option>Caller</option></select></label><label>Issue chest<input value={chest} onChange={(e) => setChest(e.target.value)} placeholder="e.g. C-04 or north island" /></label><label>Notes<input placeholder="Optional note" /></label></div><div className="modal-footer"><button className="button button-ghost" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!name.trim()} onClick={() => onSave({ name: name.trim(), role, guild: 'Coup De Grace', avatar: name.trim()[0]?.toUpperCase() || '?', tone: 'teal', status: 'Open regear', chest: chest.trim() || 'Unassigned', last: 'Not yet', issuedBy: 'Pending' })}>Add member</button></div></Modal> }

function DeathModal({ members, items, onAddItem, onClose, onSave }) { const firstMember = members[0]; const [memberName, setMemberName] = useState(''); const [memberQuery, setMemberQuery] = useState(''); const [memberSuggestionsOpen, setMemberSuggestionsOpen] = useState(false); const [role, setRole] = useState(firstMember?.role || 'DPS'); const [cta, setCta] = useState('Avalonian Roads'); const [chest, setChest] = useState(''); const [selectedItems, setSelectedItems] = useState([]); const selectedMember = members.find((member) => member.name === memberName); const memberSuggestions = members.filter((member) => `${member.name} ${member.role}`.toLowerCase().includes(memberQuery.toLowerCase())).slice(0, 6); const chooseMember = (member) => { setMemberName(member.name); setMemberQuery(member.name); setRole(member.role); setChest(member.chest); setMemberSuggestionsOpen(false) }; return <Modal title="Report a CTA death" eyebrow="Create regear request" onClose={onClose}><div className="form-grid"><div className="member-search-field"><label>Member</label><div className="member-search"><Icon name="search" size={15} /><input autoFocus value={memberQuery} onFocus={() => setMemberSuggestionsOpen(true)} onChange={(e) => { setMemberQuery(e.target.value); setMemberName(''); setMemberSuggestionsOpen(true) }} placeholder="Search character name..." /></div>{memberSuggestionsOpen && memberQuery.trim() && <div className="member-suggestion-box">{memberSuggestions.map((member) => <button type="button" key={member.name} onMouseDown={(e) => e.preventDefault()} onClick={() => chooseMember(member)}><span className={`avatar avatar-${member.tone}`}>{member.avatar}</span><span><strong>{member.name}</strong><small>{member.role} · Issue chest {member.chest}</small></span></button>)}{!memberSuggestions.length && <span className="suggestion-empty">No roster member found.</span>}</div>}</div><label>Role for this CTA<select value={role} onChange={(e) => setRole(e.target.value)}>{roleOptions.map((option) => <option key={option}>{option}</option>)}</select><small className="field-help">This can change from the member’s default role.</small></label><label>CTA event<input value={cta} onChange={(e) => setCta(e.target.value)} placeholder="e.g. Avalonian Roads" /></label><label>Issue from chest<input value={chest} onChange={(e) => setChest(e.target.value)} placeholder="Select a member to auto-fill" /></label><div className="field-full"><label>Replacement items</label><ItemAutocomplete items={items} selectedItems={selectedItems} setSelectedItems={setSelectedItems} onAddItem={onAddItem} /></div></div><div className="death-callout"><div className="death-pulse" /><div><strong>New open regear</strong><span>{selectedMember ? `${selectedMember.name} · ${role} · Chest ${chest || 'unassigned'}` : 'Search for the member who died'}{selectedItems.length ? ` · ${selectedItems.map((item) => item.name).join(', ')}` : ''}</span></div></div><div className="modal-footer"><button className="button button-ghost" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!selectedMember} onClick={() => onSave({ memberName, role, note: `Died in ${cta} · just now`, chest: chest.trim() || 'Unassigned', items: selectedItems.map((item) => item.name) })}><Icon name="swords" size={15} />Create regear request</button></div></Modal> }
function ItemAutocomplete({ items, selectedItems, setSelectedItems, onAddItem }) { const [query, setQuery] = useState(''); const [open, setOpen] = useState(false); const suggestions = items.filter((item) => item.name.toLowerCase().includes(query.toLowerCase()) && !selectedItems.some((selected) => selected.name === item.name)).slice(0, 6); const choose = (item) => { setSelectedItems((current) => [...current, item]); setQuery(''); setOpen(false) }; const addNew = () => { const name = query.trim(); if (!name) return; const item = onAddItem(name); choose(item); }; return <div className="autocomplete"><div className="token-input">{selectedItems.map((item) => <button type="button" className="item-token" key={item.name} onClick={() => setSelectedItems((current) => current.filter((selected) => selected.name !== item.name))}>{item.name}<span>×</span></button>)}<input value={query} onFocus={() => setOpen(true)} onChange={(e) => { setQuery(e.target.value); setOpen(true) }} placeholder={selectedItems.length ? 'Add another item...' : 'Type an item name...'} /></div>{open && query.trim() && <div className="suggestion-box">{suggestions.map((item) => <button type="button" key={item.name} className="suggestion-row" onMouseDown={(e) => e.preventDefault()} onClick={() => choose(item)}><span className="suggestion-icon"><Icon name={item.category === 'Weapon' ? 'swords' : 'box'} size={14} /></span><span><strong>{item.name}</strong><small>{item.category} · stored item</small></span><Icon name="plus" size={14} /></button>)}{!suggestions.some((item) => item.name.toLowerCase() === query.trim().toLowerCase()) && <button type="button" className="suggestion-add" onMouseDown={(e) => e.preventDefault()} onClick={addNew}><Icon name="plus" size={14} />Add “{query.trim()}” to item catalog</button>}{!suggestions.length && <span className="suggestion-empty">No matching item. Add it to the catalog.</span>}</div>}</div> }
function ItemModal({ onClose, onSave }) { const [name, setName] = useState(''); const [category, setCategory] = useState('Weapon'); const [chest, setChest] = useState(''); return <Modal title="Add catalog item" eyebrow="Armory catalog" onClose={onClose}><div className="form-grid"><label>Item name<input autoFocus value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Judicator Armor" /></label><label>Category<select value={category} onChange={(e) => setCategory(e.target.value)}><option>Weapon</option><option>Off hand</option><option>Head</option><option>Armor</option><option>Boots</option><option>Custom</option></select></label><label>Chest location<input value={chest} onChange={(e) => setChest(e.target.value)} placeholder="e.g. C-04 or north island" /></label><label>Starting stock<input type="number" min="0" defaultValue="0" inputMode="numeric" /></label></div><div className="kit-callout"><Icon name="box" size={18} /><div><strong>Saved to the item catalog</strong><span>Admins can use this item in future death reports and role templates.</span></div></div><div className="modal-footer"><button className="button button-ghost" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!name.trim()} onClick={() => onSave({ name: name.trim(), category, chest: chest.trim() || 'Unassigned', stock: '0 / 0', percentage: 0, tone: 'low' })}><Icon name="plus" size={15} />Add item</button></div></Modal> }
function PlanModal({ onClose, onSave }) { const [name, setName] = useState(''); const [budget, setBudget] = useState('500000'); return <Modal title="Schedule CTA" eyebrow="CTA event" onClose={onClose}><div className="form-grid"><label>CTA name<input autoFocus value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Avalonian Roads" /></label><label>Default kit template<select defaultValue="Caller standard"><option>Tank standard</option><option>Support standard</option><option>Healer standard</option><option>DPS standard</option><option>Bomb standard</option><option>Caller standard</option></select></label><label>Date<input type="date" defaultValue="2026-09-13" /></label><label>Start time<input type="time" defaultValue="20:00" /></label><label>Attendees<select defaultValue="Select members"><option>Select members</option><option>All classified members</option><option>All active members</option></select></label><label>Silver budget<input value={budget} onChange={(e) => setBudget(e.target.value)} inputMode="numeric" placeholder="500000" /></label></div><div className="kit-callout"><Icon name="swords" size={18} /><div><strong>Template for casualty requests</strong><span>When a death is reported, this template can fill weapon, off-hand, helmet, armor, and boots for that role.</span></div></div><div className="modal-footer"><button className="button button-ghost" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!name.trim()} onClick={() => onSave({ name: name.trim(), budget })}>Schedule CTA</button></div></Modal> }

createRoot(document.getElementById('root')).render(<App />)
